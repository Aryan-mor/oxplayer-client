---
description: TDLib official pin — Android/Web artifacts, no Git LFS, Dart types; on exact phrase "build sdk", run ## Build SDK automation (oxplayer-client tag rotation).
alwaysApply: true
---

# TDLib: single version across platforms

OXPlayer targets **one git pin** of [tdlib/td](https://github.com/tdlib/td) recorded in `tool/tdlib/TD_VERSION.json` (`git_ref`, `commit_sha`, `toolchain`).

- **Android**: `libtdjson.so` per ABI — **not** committed at scale; download with `dart run tool/tdlib/fetch_artifacts.dart` from your **versioned object storage / artifact registry** (paths keyed by `commit_sha`). **Git LFS is forbidden** for these binaries. The script **fails the build** (`exit 1`) if config is missing or any artifact cannot be fetched.
- **Web**: WASM/worker bundle from the **same** `commit_sha` (built via upstream `example/web` at that pin, packaged as `web/dist.tar.gz` in the registry). Legacy `npm tdweb` / `scripts/sync-tdweb.mjs` is optional for local fallback.
- **Dart `td_api` types**: generated into **`lib/td_api_generated/`** from official `td_api.tl` at the pin in `TD_VERSION.json` (`dart run tool/generator/generate.dart --fetch`). Native JSON must match that same pin.
- **JSON ↔ Dart**: `lib/oxplayer/telegram/td_json_official_client.dart` binds `td_json_client_*` via `dart:ffi` (decoupled from the Flutter plugin runtime). `TdTelegramClient` in `lib/oxplayer/telegram/tdlib_facade.dart` is the app-facing abstraction (legacy typedef `TdlibFacade`).

## Rule

1. Bump **`tool/tdlib/TD_VERSION.json`** only when Android `.so`, web bundle, and (when applicable) Dart schema are updated **together**.
2. After bump: rebuild artifacts with the pinned **NDK r26b** + **Emscripten 3.1.1** matrix in `docs/tdlib-official-build.md` / `tool/tdlib/Dockerfile`, publish to the registry, then run `dart run tool/tdlib/fetch_artifacts.dart`.
3. Smoke **Telegram login** on Android + Chrome (QR, 2FA, restore).

## Mitigations

None — keep `tool/tdlib/TD_VERSION.json`, native `.so`, WASM bundle, and `lib/td_api_generated/` in lockstep.

## Refreshing the `tdlib-artifacts-*` GitHub tag (producer workflow)

The workflow [`.github/workflows/tdlib-toolchain-producer.yml`](.github/workflows/tdlib-toolchain-producer.yml) runs on **`workflow_dispatch`** or on **push** of tag `tdlib-artifacts-*`. That tag must point at a commit that contains the **current** producer scripts (Dockerfile, `tool/tdlib/ci_build_release_artifacts.sh`, etc.). GitHub Actions checks out **the tagged commit**, not `main`.

**When to do this:** after merging fixes to `main` that must be used by the next producer run, while keeping the **same** TD pin (`commit_sha` in `TD_VERSION.json` unchanged). The tag name stays **`tdlib-artifacts-<full commit_sha>`** (must match `TD_VERSION.json` exactly — the workflow validates this).

**From repo root `oxplayer-client/`** (replace `<SHA>` with the full `commit_sha` from `TD_VERSION.json`):

```bash
TAG="tdlib-artifacts-<SHA>"
git checkout main
git pull --ff-only origin main
git tag -d "$TAG" 2>/dev/null || true
git push origin --delete "$TAG"
git tag "$TAG"
git push origin "$TAG"
```

- **Deleting the remote tag** removes the old GitHub Release attached to it (if any); the next successful run recreates the pre-release and assets.
- If the **TD pin changes**, update `TD_VERSION.json` first, then use the **new** tag name `tdlib-artifacts-<new_commit_sha>` (do not reuse the old suffix).

**Do not** move this tag to a commit whose `TD_VERSION.json` `commit_sha` does not match the tag suffix — the producer job will fail the tag check.

## Build SDK automation (`build sdk`)

When the user explicitly says **build sdk** (treat as a literal trigger phrase), the agent MUST run the following **atomic sequence** on repo **`oxplayer-client`** without asking for confirmation:

1. **Commit** — If the working tree is dirty: stage infrastructure/config changes (`git add -A` or scoped paths), then `git commit` with a concise `infra:` / `ci:` / `docs:` message. If clean, skip commit.
2. **Sync main** — `git checkout main` and `git pull --ff-only origin main`; if `main` is ahead of `origin/main`, `git push origin main`.
3. **Read** `commit_sha` from `tool/tdlib/TD_VERSION.json` (full git SHA of the pinned tdlib/td commit).
4. **Rotate the infrastructure tag** (replace `<SHA>` with that `commit_sha`):

```bash
TAG="tdlib-artifacts-<SHA>"
git tag -d "$TAG" 2>/dev/null || true
git push origin --delete "$TAG"
git tag "$TAG"
git push origin "$TAG"
```

5. **Effect** — Push of `tdlib-artifacts-<SHA>` triggers [`.github/workflows/tdlib-toolchain-producer.yml`](.github/workflows/tdlib-toolchain-producer.yml) on GitHub (Docker-backed producer). Deleting the remote tag first avoids needing `git push --force` for the tag ref.

**Constraints:** Run all `git` commands with `cwd` = `oxplayer-client` repository root. Do not change `commit_sha` in `TD_VERSION.json` during this protocol unless the user is explicitly bumping the TD pin (then use the new SHA in the tag name).
