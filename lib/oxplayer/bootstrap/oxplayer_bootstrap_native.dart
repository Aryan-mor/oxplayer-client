import 'package:fladder/bootstrap/app_bootstrap.dart';

/// Native boot: no TDLib warm-up (v2 uses claim-code auth + HTTP playback only).
Future<void> executePlatformSpecificBoot(AppBootstrapResult bootstrap) async {}
