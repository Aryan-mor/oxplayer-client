/// User-visible product name (tab title, about, etc.). Package name stays `fladder` for imports.
abstract final class OxplayerBrand {
  static const String appName = 'OXPlayer';

  /// Store listings, PWA manifest, HTML meta description, etc.
  static const String appTagline = 'Your Telegram media library.';
}
