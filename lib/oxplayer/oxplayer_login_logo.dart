import 'package:flutter/material.dart';

import 'package:fladder/screens/shared/fladder_logo.dart';

/// Login header logo (tester sign-in is on the Telegram QR — hold 5 seconds).
class OxplayerLoginLogo extends StatelessWidget {
  const OxplayerLoginLogo({super.key});

  @override
  Widget build(BuildContext context) => const FladderLogo();
}
