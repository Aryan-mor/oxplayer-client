{
  "name": "oxplayer-client",
  "private": true,
  "description": "Pins tdweb for scripts/sync-tdweb.mjs (Flutter web TDLib). Install: npm install",
  "scripts": {
    "tdlib:bootstrap": "node ./scripts/init-artifacts-config.mjs"
  },
  "dependencies": {
    "tdweb": "1.8.0"
  }
}
